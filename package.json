{
  "name": "pi-permission-gate",
  "version": "0.1.0",
  "description": "Permission gate extension for pi — confirm before dangerous tool calls",
  "type": "module",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.10",
    "typescript": "^5.6.0",
    "typebox": "*"
  },
  "scripts": {
    "check": "tsc --noEmit"
  }
}
